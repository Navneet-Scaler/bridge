# environments
venv/
.venv/
.env
__pycache__/
*.pyc

# jupyter
.ipynb_checkpoints/

# data — only the generator is committed, never its output
*.csv
*.parquet
/data/
*.sql.gz

# models — the training code is the artefact, not the binary. A .pkl cannot be
# diffed or reviewed, and is regenerable from src/modeling.
*.pkl
*.joblib

# db
pgdata/
docker-volumes/

# os / editor
.DS_Store
.vscode/
.idea/

# metabase local app data
metabase-data/
*.db

# build / tooling caches
.pytest_cache/
.ruff_cache/
dist/
build/
*.egg-info/

# generated reports and figures (regenerable from src/reporting)
/reports/
